- name: clusterha - get novello.sh script for powermanagement configuration
  become: true
  get_url:
    url: "{{courseurl}}/{{coursecode}}/novello.sh"
    dest: /root/
    mode: '0755'

- name: clusterha - run novello.sh
  become: true
  shell: /root/novello.sh

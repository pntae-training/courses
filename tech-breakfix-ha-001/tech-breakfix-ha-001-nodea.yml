- name: Install clusterha packages
  become: true
  yum:
    name:
      - pcs
      - fence-agents-all
    state: present

- name: Copy hacluster files to nodea
  become: true
  copy:
    src: /tmp/{{guid}}/{{coursecode}}/hacluster
    dest: /root/
